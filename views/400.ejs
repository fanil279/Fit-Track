<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/images/favicon.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>400</title>

    <style>
         body {
            background-color: #f8f9fa;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .error-box {
            max-width: 500px;
            background: white;
            border: 1px solid #ddd;
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .error-code {
            font-size: 3rem;
            color: #dc3545;
            font-weight: bold;
        }
        .error-message {
            font-size: 1.2rem;
            margin: 15px 0;
        }
    </style>
</head>
<body>
    <div class="error-box">
        <div class="error-code">400</div>
        <h3 class="mt-3 text-danger"><%= t('invalid_credentials_title') %></h3>
        <p class="error-message"><%= t('invalid_credentials_message') %></p>
        <a href="/login" class="btn btn-danger mt-3"><%= t('go_back_login') %></a>
    </div>
</body>
</html>