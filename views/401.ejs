<!DOCTYPE html>
<html lang="<%= currentLang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="icon" type="image/png" href="/images/favicon.png">
    <title>Not Authorized</title>
    <link rel="stylesheet" href="/css/styles.css">

    <style>
        .container {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .message-box {
            text-align: center;
            border: 1px solid #ccc;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: #f8f9fa;
        }

        .message-box p {
            font-size: 1.2rem;
            margin-bottom: 20px;
        }

        .message-box a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
        <a class="navbar-brand" href="#">FitTrack</a>
        <div class="collapse navbar-collapse justify-content-center">
            <div class="d-flex ms-auto">
                <a type="button" class="btn btn-outline-light btn-danger me-2" href="/change-lang/en">EN</a>
                <a type="button" class="btn btn-outline-light btn-danger me-5" href="/change-lang/ru">RU</a>
            </div>
        </div>
    </nav>  
    
    <div class="container">
        <div class="message-box">
            <p><%= t('message_not_authorized') %></p>
            <a href="/login" class="btn btn-primary btn-lg mb-3"><%= t('login') %></a>
            <p><%= t('login_or_register') %></p>
            <a href="/register" class="btn btn-success btn-lg"><%= t('register_now') %></a>
        </div>
    </div>
</body>
</html>